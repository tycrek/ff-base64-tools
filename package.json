{
	"scripts": {
		"build:render": "",
		"build:compile": "web-ext build --overwrite-dest",
		"build": "pnpm run build:render && pnpm run build:compile"
	},
	"devDependencies": {
		"pagery": "^0.6.5",
		"web-ext": "^7.8.0"
	}
}